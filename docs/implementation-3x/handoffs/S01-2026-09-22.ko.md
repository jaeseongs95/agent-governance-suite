# S01 인계 — BLOCKED_ENV

- Task ID: S01
- 제목: baseline 순위 메타데이터 접근점
- 결과 상태: BLOCKED_ENV
- 요청 모델 / 추론: gpt sol / high
- 실제 관측: GPT-5.6 Sol. 이 실행 surface에서는 reasoning effort 설정값을 독립적으로 확인하지 못했다.
- 계획 예산 / 상한: 20분 / 30분
- repository: jaeseongs95/agent-governance-suite
- branch: codex/v260-semantic-decision-layer

## 기준 identity

- 계획 기준 commit: `829464ddb39ea022df5c7f3e163c8b547940ccbb`
- 작업 시작 원격 HEAD: `65a1a3432d931ee87c572d0ec130861a1f1e9ec8`
- 작업 시작 tree: `7fe3b62f33af8093f87cab05e6898df9f2daaed8`
- 시작 HEAD는 계획 기준보다 1 commit 전진했으나 commit `65a1a343...`은 파일 변경이 없는 S01 baseline snapshot이며 tree는 계획 기준과 동일했다.
- 시작 시 대상 blob:
  - `skills/coordinate-subagents/scripts/model-routing-core.mjs`: `4ab71db3d0bebe75911145325cdd8fd72fbdf719`
  - `skills/coordinate-subagents/scripts/model-routing-core.d.mts`: `406c80304ff51c18a2fa0e17f76628096eac1cb9`

## 확인한 선행 evidence와 현재 구조

- S01은 선행 task가 없는 첫 구현 카드다.
- 직전 S1b 문서 `docs/semantic-decision/eligible-candidates-S1b.ko.md`를 읽었다.
- 현재 `rankBaselineCandidatesV2`의 기존 순서는 사용자 preference group → task-trait seed → profile/role seed → host native-control → candidate key다.
- `SemanticEligibleCandidateV1` 및 `semantic-decision-request.v1.schema.json`은 `preferenceGroup`과 `baselineRank`을 별도 정수 metadata로 이미 정의한다.
- 기존 runtime bundle `mcp-server/dist/model-routing-host-hook.mjs` 안에 `rankBaselineCandidatesV2` 구현이 실제 포함되어 있음을 readback으로 확인했다. 따라서 core 구현 변경은 source 두 파일만 바꾸고 끝낼 수 없고 repository build 절차로 파생 bundle을 재생성·검증해야 한다.

## 환경 확인 결과

- root `AGENTS.md` 확인 완료.
- `package.json`: Node >=22.13, pnpm@11.19.0.
- 실행 환경 Node: `v22.16.0`.
- Corepack: `0.32.0`.
- `pnpm`은 설치되어 있지 않다.
- `corepack pnpm --version`은 `registry.npmjs.org` 조회에서 `EAI_AGAIN`으로 실패했다.
- 따라서 frozen dependency 설치와 아래 필수 검증을 현재 실행 surface에서 수행할 수 없다:
  - `pnpm bundle:check`
  - S01 focused Vitest / legacy golden / type parity
  - `pnpm exec tsc --noEmit`
  - `pnpm build`
  - `pnpm source:check`
  - build 후 `pnpm bundle:check`
- 기존 GitHub CI는 `workflow_dispatch`를 지원하지만 현재 제공된 GitHub connector에는 workflow dispatch 동작이 없다.
- 기존 `semantic-s1b-verification.yml`은 S1b 전용 apply/bootstrap 경로이며 S01 검증용으로 재사용하지 않았다. S01 범위를 벗어난 workflow 수정이나 opaque payload 경로도 사용하지 않았다.

## 수행 결과

제품 코드 변경은 시작하지 않았다. core만 수정하면 committed runtime bundle과 source가 불일치하게 되므로, 검증 환경이 없는 상태에서 source-only commit을 남기지 않았다.

- 구현 파일 변경: 없음
- focused test 추가: 없음
- 생성 bundle 변경: 없음
- main/tag/release/설치 cache 변경: 없음
- VibeMarshal 변경: 없음
- provider/live 호출: 없음

## commit / tree 구분

- local_commit: 없음 — 로컬 Git checkout을 만들지 않았다.
- pushed_commit: 이 인계 문서만 추가하는 docs-only commit.
- tested_commit: 없음.
- tested_tree: 없음.
- verified_remote_tree: 인계 commit 이후 원격 readback으로 별도 기록한다.
- 제품 sourceRelevant tree는 시작 tree에서 변경하지 않았다.

## 남은 제약과 다음 READY

S01을 완료하려면 먼저 frozen dependency와 repository build/test를 실제 실행할 수 있는 환경을 확보해야 한다. 환경 준비는 제품 소스 변경과 분리한다.

- 다음 READY ID: **X01 — 환경 bootstrap 또는 기준선 복구**
- X01 완료 뒤 새 세션에서 S01을 다시 시작한다.
- S01 구현 시 기존 rank 계산을 공유하는 읽기 전용 `getBaselineCandidateMetadataV2`를 추가하고, 기존 v2 golden payload/canonical/digest 불변을 직접 검증해야 한다.
