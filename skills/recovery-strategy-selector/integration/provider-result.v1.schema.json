{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-governance-skills.local/recovery-strategy-selector/integration/provider-result.v1.schema.json",
  "title": "ProviderResult.v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["kind", "output", "artifacts", "error"],
  "properties": {
    "kind": { "enum": ["output", "adapter-error"] },
    "output": { "type": ["object", "null"] },
    "artifacts": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["artifactId", "schemaId", "locator", "digest", "targetDigest", "verified"],
        "properties": {
          "artifactId": { "type": "string", "minLength": 1 },
          "schemaId": { "type": "string", "minLength": 1 },
          "locator": { "type": "string", "minLength": 1 },
          "digest": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
          "targetDigest": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
          "verified": { "type": "boolean" }
        }
      }
    },
    "error": {
      "oneOf": [
        { "type": "null" },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["code", "message", "details"],
          "properties": {
            "code": { "enum": ["INVALID_INPUT", "MISSING_EVIDENCE", "GATE_FAILED", "MCP_UNAVAILABLE"] },
            "message": { "type": "string", "minLength": 1 },
            "details": { "type": ["object", "null"] }
          }
        }
      ]
    }
  }
}
